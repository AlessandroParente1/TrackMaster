services:
  - type: web
    name: nextjs-static
    runtime: static
    plan: starter
    buildCommand: yarn;yarn build
    startCommand: out
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production